import { LongTextDirective } from './long-text.directive';

describe('LongTextDirective', () => {
	it('should create an instance', () => {
		const directive = new LongTextDirective();
		expect(directive).toBeTruthy();
	});
});
